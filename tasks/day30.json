{
  "day": 30,
  "title": "CV阶段综合项目",
  "goal": "独立完成 数据→模型→训练→评价→结果展示 的完整小项目",
  "difficulty": 4,
  "estimated_minutes": 120,
  "prerequisites": [
    "CNN训练闭环",
    "checkpoint保存恢复",
    "yaml配置读写"
  ],
  "learn": [
    "配置驱动实验",
    "最优权重跟踪",
    "结果可复现性(加载权重复现acc)",
    "流水线阶段划分"
  ],
  "review_points": [
    "FastAPI",
    "endpoint"
  ],
  "required_api": [
    {
      "name": "TrainConfig",
      "signature": "TrainConfig(epochs:int, lr:float, batch_size:int, num_classes:int=2, out_path:str='best.pt')",
      "description": "dataclass；含from_dict/to_dict；缺必需字段抛KeyError或TypeError"
    },
    {
      "name": "save_config/load_config",
      "signature": "save_config(config, path); load_config(path)",
      "description": "yaml持久化roundtrip，字段无损"
    },
    {
      "name": "make_loaders",
      "signature": "make_loaders(n_train=32, n_val=8, batch_size=8) -> tuple",
      "description": "返回(train_loader, val_loader)；合成可分两类(3,32,32)，两集合互斥，禁止下载真实数据集"
    },
    {
      "name": "build_model",
      "signature": "build_model(num_classes=2) -> nn.Module",
      "description": "小型CNN"
    },
    {
      "name": "run_pipeline",
      "signature": "run_pipeline(config) -> dict",
      "description": "按配置执行建数据→建模型→训练→验证→保存最优权重到out_path，返回{'train_loss','val_loss','val_acc','model_path'}"
    },
    {
      "name": "training_curve",
      "signature": "training_curve(model, train_loader, val_loader, epochs=3, lr=0.05) -> dict",
      "description": "逐epoch训练并记录，返回{'losses':[每epoch训练loss], 'val_accs':[每epoch验证acc]}"
    },
    {
      "name": "save_prediction_grid",
      "signature": "save_prediction_grid(model, loader, out_path, n=4) -> str",
      "description": "用matplotlib(Agg)把前n张图的预测/真实标签渲染成网格图保存到out_path并返回路径"
    }
  ],
  "core_task": "实现TrainConfig(yaml持久化)/make_loaders/build_model/run_pipeline。run_pipeline按配置执行全链并保存最优权重；核心约90分钟内可完成，进阶优化放挑战",
  "mastery": [
    "能用配置对象驱动一次完整实验",
    "能保证最优权重落盘且加载后复现val_acc",
    "能把数据、训练、评估拆成职责清晰的阶段函数",
    "能产出训练曲线与预测可视化两类结果展示物"
  ],
  "optional_challenge": "挑战：单batch过拟合实验验证容量。挑战进阶：run_pipeline支持可选的轻量增强开关并对比val_acc差异",
  "required_output": [
    "yaml训练配置文件",
    "training curve(逐epoch loss曲线数据)",
    "validation metric(val_acc)",
    "最优权重文件(model_path)",
    "prediction visualization(预测可视化图)"
  ],
  "environment_notes": "环境要求：全程CPU；仅本地文件读写（yaml配置与最优权重）。",
  "skills": [
    "engineering.config",
    "application.pipeline",
    "cv.classification",
    "evaluation.accuracy",
    "pytorch.training_loop",
    "pytorch.checkpoint",
    "cv.visualization"
  ],
  "tests": [
    "test_answer_module_imports",
    "test_loaders_structure",
    "test_loss_curve_decreases",
    "test_missing_key_raises",
    "test_pipeline_report_and_artifact",
    "test_prediction_grid_saved",
    "test_roundtrip",
    "test_saved_model_reproduces_val_acc",
    "test_separable_data_learns",
    "test_training_curve_structure",
    "test_yaml_persistence"
  ],
  "test_module": "day30_test",
  "task": "配置驱动的图像分类端到端流水线",
  "description": "CV阶段毕业项目：不引入新知识，只考核D11-D29能力的组装——配置驱动建数据、训小CNN、验证评估、最优权重落盘并可复现。",
  "hints": [
    "设计应用架构",
    "集成模型推理",
    "添加日志和监控"
  ],
  "hint_levels": [
    {
      "level": 1,
      "content": "项目骨架五个函数正好对应五个阶段：配置、数据、模型、训练验证、产物"
    },
    {
      "level": 2,
      "content": "run_pipeline内部调用make_loaders与build_model，保持函数间只通过config与返回值耦合"
    },
    {
      "level": 2,
      "content": "最优权重跟踪：val_loss创新低时torch.save(state_dict)到out_path"
    },
    {
      "level": 2,
      "content": "training_curve复用训练循环，每个epoch结束后记录当前loss与val_acc"
    },
    {
      "level": 2,
      "content": "save_prediction_grid用matplotlib.use(Agg)后端，subplot网格标题写预测与真实标签"
    },
    {
      "level": 3,
      "content": "判据：加载model_path权重后在同一验证集上acc与报告值误差小于1e-6"
    }
  ],
  "review_points_replace": [
    "training loop",
    "validation",
    "checkpoint",
    "augmentation"
  ]
}
