[pytest]
testpaths = tests
addopts = --basetemp=.pytest_tmp
markers =
    skill(...): curriculum skill tags for Learning Agent filtering
    challenge: optional challenge test (fails do not affect day pass/fail)
